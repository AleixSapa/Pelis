services:
  pelitrack:
    build: .
    container_name: pelitrack
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      # La base de dades queda al servidor i no dins del contenidor.
      # Així sobreviu quan es recrea o actualitza el contenidor.
      - ./pelitrack-data:/app/data

